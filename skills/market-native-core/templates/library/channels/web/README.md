# web

Written copy for the website lives in `copy/<locale>/`. Length limits per locale are in
`limits.json`; measure them on the real layout. Blueprints come from the `website-copy`
adapter skill; a file in `blueprints/<page>.md` here overrides the skill's default for this
project.
